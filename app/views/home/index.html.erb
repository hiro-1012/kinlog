<div class="hero d-flex align-items-center text-center">
  <div class="container">
    <h1 class="hero-title mb-3">筋ログ</h1>
    <p class="hero-lead mb-4">
      トレーニングの重さ×回数（ボリューム）を記録して、<br class="d-none d-md-inline">
      前回越えを目指すシンプルな記録アプリ。
    </p>

    <% unless user_signed_in? %>
      <div class="btn-group">
        <%= link_to '新規登録', new_user_registration_path, class: 'btn btn-primary btn-lg' %>
        <%= link_to 'ログイン',   new_user_session_path,   class: 'btn btn-outline-primary btn-lg' %>
      </div>
    <% end %>
  </div>
</div>